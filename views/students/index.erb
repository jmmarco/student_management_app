<div class="container">
  <div class="col-md-12">
    <h2>All students</h2>
    <ul>
    <% @students.each do |student| %>
      <li><a href="/students/<%= student["id"]  %>"><%= student["name"] %></a></li>
    <% end %>
    </ul>
  </div>
</div>